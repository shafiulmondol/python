#syntex= string.startswith(prefix,start,end). start and end is optional 
# start is starting endex and end is ending index
str='shafiul mondol'
print(str.startswith('sha'))
print(str.startswith('ul',5,15))
print(str.startswith(('sha','boy')))
